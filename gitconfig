[user]
	name = Matt Campbell
	email = xenolinguist@gmail.com
[github]
	user = xenolinguist
[color]
	ui = auto
    status = auto
    commit = auto
    diff = auto
    branch = auto
[alias]
    st = status
    co = checkout
    ci = commit
    cp = cherry-pick
    br = branch
    shove = push --force
    fixup = commit --amend -C HEAD
    fall = fetch --all --prune
    up = rebase origin/master
[credential]
	helper = cache
[status]
	displayCommentPrefix = true
[filter "lfs"]
	clean = git-lfs clean %f
	smudge = git-lfs smudge %f
	required = true
